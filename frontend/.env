# Live backend on Render. NOTE: Vite bakes this in at BUILD time — changing it
# later requires a rebuild, not just a restart.
VITE_API_BASE_URL=https://relay-api-ogj6.onrender.com

# VITE_ROLE_STUB removed: it overrode the server's role and forced every user to
# admin, making role-based UI impossible to test. /api/me/ now returns a real role.
